name: Greetings
run-name: Say hello to contributor
on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: 'say hi'
        run: echo "Hello ${{ github.actor }}"
      - uses: mshick/add-pr-comment@v2
        with:
          message: |
            **Hello**
            🌏
            !